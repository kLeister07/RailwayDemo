package com.example.railway_backend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.example.railway_backend")
public class RailwayBackendApplication {
	public static void main(String[] args) {
		SpringApplication.run(RailwayBackendApplication.class, args);
	}
}

